# set the default temperature measurement unit
# you can specify on of [c (celcius)),f (farenheith)]
default-temperature-measurement=c

# specify if symbol of temperature measuremente must show
show-symbol=1